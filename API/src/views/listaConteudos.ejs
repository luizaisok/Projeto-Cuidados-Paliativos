<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Lista de Conteúdos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container my-5">
    <h1 class="mb-4 text-primary">Conteúdos da API</h1>

    <% conteudosDoController.forEach(c => { %>
      <div class="card mb-3 shadow-sm">
        <div class="card-body">
          <h5 class="card-title"><%= c.id %> - <%= c.titulo %></h5>
          <p class="card-text"><strong>Descrição:</strong> <%= c.descricao %></p>
          <p class="card-text"><strong>Texto:</strong> <%= c.texto %></p>
          <p class="card-text text-muted"><strong>Data:</strong> <%= c.data %></p>
        </div>
      </div>
    <% }) %>

    <a href="/" class="btn btn-secondary mt-4">Voltar à Home</a>
  </div>
</body>
</html>